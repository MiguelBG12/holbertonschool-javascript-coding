// Declaration of a function named displayMessage that takes a parameter called message.
function displayMessage(message) {
  // We use the `console.log()` function to print the message to the standard output (STDOUT).
  console.log(message);
}

// We export the displayMessage function so it can be used in other files.
module.exports = displayMessage;
